# Copy this file to .env.test.local and fill in real credentials.
# These accounts must already exist in your Firebase project.

TEST_LEADER_EMAIL=leader@example.com
TEST_LEADER_PASSWORD=your_leader_password

TEST_MEMBER_EMAIL=member@example.com
TEST_MEMBER_PASSWORD=your_member_password

# Defaults to http://localhost:3000
PLAYWRIGHT_BASE_URL=http://localhost:3000
